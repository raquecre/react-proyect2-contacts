//import React from 'react';
//import PropTypes from 'prop-types';
//import styles from './AddRandomContact.scss';
//import contactsJSON from "./contacts.json"

/* const AddRandomContact = () => {
	const [contacts, setContacts] = useState(contactsJSON);
	
	const randomPosition = () => Math.floor(Math.random() * contactsJSON.length);
	const randomContact = contactsJSON[randomPosition]

	const addRandomContact = (contacts) => (randomContact.id !== contacts.id) ? setContacts(randomContact)
	
}; */





//export default AddRandomContact;
